mixin icon(name, mod)
  if(mod)
    svg(class="icon icon-" + name + ' ' + mod)
      use(href="img/symbols.svg#" + name)
  else
    svg(class="icon icon-" + name)
      use(href="img/symbols.svg#" + name)